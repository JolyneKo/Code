# Lista é um conjunto de valores que podem ser guardados em uma variável

# Com a lista, é possível guardar vários valores em uma variável

lista = [1, "string", True, 3.14]

# A lista pode guardar díferentes tipos de dados dentro dela

# Uma lista é feita de uma sequência de itens separados por vírgula.
# Uma lista é representada pelos colchetes []

# Listas são mutável, é possível mudar diretamente os elementos.

# Cada elemento tem um index (uma posição), começando por zero

nums1 = [0, 1, 2, 3, 4]

# Python começa a contar por zero, e cada elemento em um index começando por zero.

# É possível extrair valores de uma tupla, e colocar em variáveis

# Unpacking

a, b, c, d, e = nums1

# Variáveis tem escopo de função

# Criadas fora de uma função são variáveis globais
# Criadas dentro, são variáveis locais

# Para usar variáveis globais dentro de uma função, usa a palavra-chave global


# Tuplas é uma sequência de elementos, imutável, que pode guardar vários tipos de dados.
# Cada elemento é separado com uma vírgula
# Não é possível modificar uma tupla (constante, imutável)
# Uma tupla é representada por parênteses ()

tupla = (1, 2, 3, 4, 5)

# Cada elemento tem uma posição (index), e python conta a posição começando por zero

# A primeira posição é o index 0
